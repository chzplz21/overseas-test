 //Handles toggle down for hotel container
 var RoomObject = function(listenedElement, revealedElement, ajaxURL, searchedForElement) {
    HandlingEvents.call(this, listenedElement, revealedElement, ajaxURL, searchedForElement);
    this.RoomContainer;
}

RoomObject.prototype = new HandlingEvents();

RoomObject.prototype.InstantiateNext = function() {}













